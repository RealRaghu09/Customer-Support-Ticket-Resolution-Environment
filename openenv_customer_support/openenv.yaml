name: email-triage-env
description: Email classification and response environment

actions:
  category: string
  decision: string
  reply: string

observations:
  subject: string
  body: string

reward:
  score: float